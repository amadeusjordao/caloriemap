<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>CalorieMap</title>

    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
        }

        header {
            background: #6a5acd;
            color: white;
            padding: 15px;
            text-align: center;
            font-size: 22px;
            font-weight: bold;
        }

        .container {
            width: 90%;
            max-width: 420px;
            background: white;
            margin: 25px auto;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0px 0px 10px #0002;
        }

        input, button {
            width: 100%;
            padding: 12px;
            margin-top: 10px;
            border-radius: 8px;
            border: 1px solid #aaa;
            font-size: 15px;
        }

        button {
            background: #6a5acd;
            border: none;
            color: white;
            font-weight: bold;
            cursor: pointer;
        }

        button:hover {
            background: #5847c0;
        }

        .hidden {
            display: none;
        }

        .result {
            margin-top: 15px;
            padding: 15px;
            background: #eee;
            border-radius: 8px;
        }
    </style>
</head>
<body>

<header>CalorieMap</header>

<div class="container" id="loginBox">
    <h3>Login</h3>
    <input id="loginEmail" placeholder="Email" />
    <input id="loginSenha" type="password" placeholder="Senha" />
    <button onclick="login()">Entrar</button>
    <button onclick="abrirCadastro()">Criar conta</button>
</div>

<div class="container hidden" id="cadastroBox">
    <h3>Criar Conta</h3>
    <input id="cadEmail" placeholder="Email" />
    <input id="cadSenha" type="password" placeholder="Senha" />
    <button onclick="cadastrar()">Cadastrar</button>
    <button onclick="abrirLogin()">Voltar</button>
</div>

<div class="container hidden" id="home">
    <h3>Buscar calorias</h3>
    <input id="foodInput" placeholder="Digite o alimento..." />
    <button onclick="buscarCalorias()">Calcular</button>

    <div id="result" class="result hidden"></div>
</div>

<script>
    // ---- LOGIN / CADASTRO ----
    function abrirCadastro() {
        document.getElementById("loginBox").classList.add("hidden");
        document.getElementById("cadastroBox").classList.remove("hidden");
    }

    function abrirLogin() {
        document.getElementById("cadastroBox").classList.add("hidden");
        document.getElementById("loginBox").classList.remove("hidden");
    }

    function cadastrar() {
        let email = document.getElementById("cadEmail").value;
        let senha = document.getElementById("cadSenha").value;

        if (!email || !senha) {
            alert("Preencha tudo");
            return;
        }

        localStorage.setItem("user_email", email);
        localStorage.setItem("user_senha", senha);

        alert("Conta criada!");
        abrirLogin();
    }

    function login() {
        let email = document.getElementById("loginEmail").value;
        let senha = document.getElementById("loginSenha").value;

        if (
            email === localStorage.getItem("user_email") &&
            senha === localStorage.getItem("user_senha")
        ) {
            document.get
